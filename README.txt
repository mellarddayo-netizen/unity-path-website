UNITY PATH FINAL HERO FIX

Desktop hero now uses the complete wide hero artwork as one image.
This prevents the browser from cropping/repositioning the artwork differently.
Replace the existing index.html and hero-desktop.jpg together.
Keep the other image files in the same repository root.
For mobile, the existing mobile-specific layout remains responsive.
